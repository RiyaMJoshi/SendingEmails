{% apply inky_to_html|inline_css(source('@styles/foundation-emails.css'), source('@styles/email.css')) %}
    <container>
        {# Header #}
        <hr>
        <spacer size="20"></spacer>
        <row>
            <columns>
                <p>
                    What a week {{ email.toName }}! Here's a quick review of what you've been up to on the Space Bar this week
                </p>
            </columns>
        </row>
        <row>
            <columns>
                {{ include('emails/_report-table.html.twig') }}
            </columns>
        </row>
        <row>
            <columns>
                <center>
                    <spacer size="20"></spacer>
                        <a href="{{ url('app_home') }}" class="btn btn-home">Check on the Space Bar</a>
                    <spacer size="20"></spacer>
                </center>
            </columns>
        </row>
        {# Footer #}
    </container>
{% endapply %}
